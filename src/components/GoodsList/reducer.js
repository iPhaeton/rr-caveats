const initialState = {
    names: [],
    prices: [],
    updatedAt: null,
};

const goods = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default goods;